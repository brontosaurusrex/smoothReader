#!/bin/sh
set -eu

# Environment variables such as PIPER_BIN, FFMPEG_BIN, PIPER_VOICE_DIR,
# PIPER_CACHE_DIR, and SMOOTH_READER_LIBRARY_DIR are inherited automatically.
# Extra bridge options can be passed directly to this script.
exec ./piper_bridge.py "$@"
